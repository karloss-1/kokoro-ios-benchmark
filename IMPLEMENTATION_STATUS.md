# Implementation status

## Phase closed for physical-device testing — 2026-09-25
- This is the current handoff state; older entries below are chronological records, not an instruction to resume their former next steps. No further synthetic QA is planned for this phase. No essential-flow production blocker is known.
- Final finite smoke coverage accepted from recent passing evidence, as authorized: Library launch, Add Text, Reader, Play/Pause state, sentence/paragraph navigation, restored position and Settings passed in `testTextReaderAndSettings` on iPad Simulator (`/private/tmp/reader-ipad-fixed.log`). Character-weighted semantic seek passed in the PDF range UI regression; actual Export Text contents passed in the selected-Two EPUB regression (`/private/tmp/reader-all-chapters-fixed.log`). The 390 pt accessibility navigation test also passed (`/private/tmp/reader-accessible-layout-fixed.log`). No production code changed after these relevant checks, except the already-tested accessibility layout relative to the earlier seek/export checks. No new test matrix was run at closure; audible voice quality remains a physical-device check.
- Final clean production-project build passed: Xcode 27, generic iOS Simulator destination, signing disabled, `clean build`, exit 0 (`/private/tmp/reader-final-build.log`). No Swift warnings/errors; only the informational AppIntents metadata-extraction warning because this app does not link AppIntents. Deployment target remains iOS/iPadOS 26. Only iOS 27 Simulator was available.
- iPad Files/EPUB accessibility automation remains deferred and incomplete, including the later missing All chapters query. It has not established a production bug. No production code or XCTest changes were made during final closure.
- Known practical limits: extraction/reading order and handwriting accuracy depend on the document; protected inputs are rejected; large books need device memory/performance checks. AVAudioSession emitted a main-thread activation/deactivation performance warning during prior runs, without an observed essential-flow blocker. Device-only audio behavior is unverified.
- README contains installation/signing instructions and the physical checklist: real PDFs/EPUBs/photos/scans, voices and Enhanced quality, speech/navigation/seek/highlighting/auto-scroll, rate changes, persistence/export, lock/background/Now Playing/remote commands/interruptions, large books and offline use with available voice resources.
- Final checkpoint and GitHub feature-branch snapshot are prepared from this versioned state. Publication verifies equality of the complete local and remote Git tree; the resulting commit identifiers are reported in the handoff. Main must remain `5a49a6959418e2a49ecdb3e7f2c3e55593f13d02`.

## Phase 0 — audited (2026-09-24)
- Baseline: `5a49a69`. User confirms native TTS works on a real iPhone.
- Baseline builds with Xcode 27.0 (27A266a), iOS Simulator SDK 27, signing disabled.
- SpeechEngine owns one persistent AVSpeechSynthesizer and queues one bounded utterance at a time. Delegate identity checks reject obsolete callbacks. Preserve this behavior.
- Audio session: playback / spokenAudio / no mixing; UIBackgroundModes=audio. Preserve these settings.
- Installed voices expose name, locale, ID and quality. Existing rate mapping = default rate × approximate multiplier, clamped to public limits.
- No persistence, imports, media controls, entitlements or external dependencies exist yet. Benchmark UI and test corpus are product scaffolding only.
- Project uses Xcode automatic schemes: do not reintroduce a handcrafted shared scheme.
- Local user's checkout has Xcode formatting/signing changes. Implementation is isolated in a Git worktree; user signing settings are not copied or committed.

## Plan / architecture
- iOS/iPadOS 26 minimum, Swift 6. SwiftData metadata + application-managed source/JSON/thumbnail files.
- One normalized section/page → paragraph → sentence representation feeds Reader, TTS, progress and export. No synthetic pages for EPUB or text.
- PDFKit embedded text per page, Vision RecognizeDocumentsRequest for pages/images requiring OCR; VisionKit scanner, PhotosPicker.
- Readium Swift Toolkit stable 3.11.0 (verified release), SPM Shared/Streamer products for EPUB parsing/content only.
- SwiftUI Library and Settings tabs, serif Reader, green native cards matching supplied references.
- AVSpeechSynthesizer preserved/evolved; semantic seeking (not an audio timeline), MediaPlayer remote controls.

## Completed / remaining
- Implemented, with validation in progress: Phases 0–7. SwiftData metadata, JSON content, saved sentence position/settings, native Library/Settings/Reader, rename/delete/export. Existing audio-session behavior retained.
- Xcode 27 simulator build passed with Swift 6 / iOS 26 minimum. No Swift compiler warnings; only Xcode’s informational AppIntents metadata warning.
- Semantic navigation/seek and sentence highlighting are implemented early as part of the shared-model Reader; device behavior is not yet verified.
- Phase 3: PDF range options, security-scoped source copying, per-page embedded text/OCR, original page boundaries, thumbnails, actual progress and cancellation. Xcode 27 simulator build passed.
- Phase 4: ordered PhotosPicker import and VisionKit document camera, sequential ImageIO downsampling/OCR, source-page storage and cancellation. Build verified; real camera/photo access pending device validation.
- Phase 5: Readium 3.11.0 pinned via SPM; real TOC/chapter selection, local-only asset access, cover and semantic text extraction. No Readium Navigator/TTS/server adapter linked. Missing TOCs use explicitly labeled spine sections. Ambiguous anchor boundaries reject partial import rather than guessing; All chapters keeps all text. Simulator build passed.
- Phases 6–7: sentence/paragraph controls, semantic character-weighted seek, sentence highlighting, auto-scroll, saved position, Now Playing and remote play/pause/previous/next sentence. No fake duration/elapsed timestamps published. Preserved background audio. Simulator build passed.
- Recovery checkpoint (2026-09-24): the six implementation commits after `5a49a69` are intact on `codex/native-reader`. The two `Tests/` files are the unfinished integration harness from the interrupted run; they are preserved without changes to their test logic.
- Mixed PDF resolved: recovered `mixed.pdf` was zero bytes, generated by a second call to the same UIGraphicsPDFRenderer. A dedicated renderer now produces the fixture; assertions verify two pages and no embedded text on page 2. PDFKit + Vision extraction passes. Removed the catch that allowed false overall success. Full core integration executable passed on iOS 27 Simulator (2026-09-24).
- Current build: recovered app compiled successfully with Xcode 27 before changes. Next: XCTest UI flows, remaining integration/error review, README and polish. README still describes the previous benchmark.
- Real device tests pending: all new workflows, camera, voice availability, background/lock screen, interruptions and remote commands.
- No new functionality claimed as device-tested. Xcode GUI is not automated in this environment.

## Continuation — 2026-09-24
- Fixed completed-position loss: Stop and rate changes no longer lower a completed document’s progress. Reproduced by a failing regression check, then verified passing in the integration executable. Rename also updates the active system-media title; stale pause callbacks cannot undo a resume request.
- Current app build: Xcode 27 simulator build passed after playback fixes. Core integration passes, including PDF corruption rejection and mixed PDF OCR.
- XCTest temporary host now drives the real app. Confirmed Add Text, Reader, sentence/paragraph controls, Play/Pause state, relaunch position, Settings/appearance/voice list. Rename/export/delete and import UI validation are still in progress. The application project retains automatic schemes.
- README rewritten for the current reader. Next: finish UI coverage, accessibility/390pt/iPad review and final build; publish a new remote snapshot after checks.

## Checkpoint on interruption — 2026-09-24
- Local HEAD before this checkpoint: `3652597` (`Preserve completed reading position across playback controls`). `main` / `origin/main` remain at `5a49a69`; no branch reconciliation was performed.
- Preserved uncommitted work from the interrupted continuation: README now describes the native document reader; Library menu hit target is 44 pt; Reader uses Dynamic Type scaling and hides the tab bar; document file protection is applied before child files are written; image OCR progress reports completed pages; Processing content scrolls with a bottom-pinned Cancel action; integration checks add corrupt image/EPUB and multiple-image/order cases; temporary-copy XCTest UI harness exercises text → Reader → Settings → Library flows and opens the Files picker. These latest UI layout and integration additions were made after the last UI test process had copied its inputs, so they remain unvalidated.
- Local Xcode UI run ended (no xcodebuild/XCTest process remains). The latest saved run used the separated Rename/Deletion presentation booleans. It failed at the Rename UI assertion: XCTest had verified the text field held `Renamed anatomy`, but the final captured hierarchy still showed the Rename alert open and the library title unchanged. Thus rename through the UI is unresolved; do not claim Export Text/Delete were revalidated because this failure occurred before those steps.
- The earlier integration executable pass for the required PDF mixed text/OCR case remains valid. The newly added corrupt image/EPUB and multi-image assertions have not run. The prior app simulator build passed before the latest uncommitted Processing layout edit; that edit and the other latest changes need a build on continuation.
- Resume with diagnosing the Rename alert/action from the captured XCTest result, then rerun Rename → search → Export Text → Delete. Rebuild before treating the latest UI changes as validated; then resume the remaining import UI checks and device-only checklist. No physical-device validation has occurred.

## Resume QA — 2026-09-24
- Current-source simulator build passed, including the Processing/Dynamic Type changes. The expanded core integration run passed all 37 assertions, including corrupt images/EPUBs, no-TOC extraction and multi-image page markers.
- Rename failure diagnosed in XCTest interaction: synthetic typing left the software keyboard hidden; the next tap made it appear and moved the alert from Save center y=506 to y=359. Captures before/after and synthesized events show the mismatch. The test now refocuses the field and waits for the software keyboard before tapping Save. With the existing application code, the alert closes, Library updates and the new title persists across relaunch. No new app-side rename workaround was required.
- UI harness now uses a stable temporary source directory for incremental builds, and search to locate newly added rows outside LazyVStack's current viewport. Production project/schemes remain unchanged.
- Rename → search → actual Export Text Save → Delete → relaunch passed in XCTest. The exported UTF-8 file exactly matched the normalized two-paragraph test text. A separate read-only SQLite/filesystem check confirmed all pre-existing metadata remained unchanged and the new document's managed data was removed. The harness now resolves the data container again after XCTest, which can reinstall the app and relocate that container.
- Next: PDF/EPUB/photo import UI, remaining visual/accessibility checks and final build. Physical device checklist remains pending.

## Recovery checkpoint — after PDF UI attempt (2026-09-24)
- Current validated checkpoint before this recovery: `9d33d5c` (`Validate rename persistence, actual text export and document deletion`). `main` and `origin/main` are unchanged at `5a49a6959418e2a49ecdb3e7f2c3e55593f13d02`.
- The earlier 37 core integration checks and current-source simulator build passed. Rename, actual text export, Delete, relaunch persistence and preservation of older library entries passed as recorded above.
- The exploratory Files UI test completed and reached the Files browser. The subsequent PDF UI test selected `embedded.pdf` from `On My iPhone / Document Reader / Validation fixtures`; the app presented PDF options. The test tapped “Page range”, but the `From` and `To` fields still appeared `Disabled`. The next attempt to type into `From` failed with XCTest “Neither element nor any descendant has keyboard focus”; the run ended with test exit 65. This is the exact stopping point; no range was changed, PDF import was not started, and no PDF Reader/seek/export assertions ran. No conclusion is drawn about a production PDF-range bug from this failed automation interaction.
- The unfinished PDF XCTest and its Files helper are preserved in `Tests/ReaderUITests.swift` in the recovery commit. Do not mark PDF UI import as passed. Resume by checking the Page range control and enabled state in the captured PDF options screen, then continue only after the range inputs can be operated.
- The xcodebuild/XCTest invocation has returned failure and is no longer running as that command. A detailed process listing was unavailable: macOS denied `ps` in the sandbox, and the required permission review was declined because the account had reached its usage limit. No processes were killed. The iPhone 18 Pro simulator was the selected target; no project files were changed by process inspection.
- Latest source change since `9d33d5c` was the unfinished UI test described above; application source and functionality were not changed during this recovery.

## PDF range QA — 2026-09-25
- Reproduced a real hit-area issue: tapping the Page range row center left the fields disabled; tapping its visible label enabled them with unchanged app code. The plain HStack's transparent spacer was outside its hit shape. Added an explicit rectangular hit shape and 44 pt minimum height to that control.
- Regression XCTest passed with the original center tap, All pages toggle, invalid lower bound (Import disabled), pages 2–3 import, original page labels in Reader, semantic slider to page 3, Contents navigation back to page 2, and actual Export Text Save. The saved UTF-8 file in Files/Validation fixtures contains [Page 2] and [Page 3] with their extracted text and excludes page 1.
- Test build passed with the correction. EPUB selected-chapter UI QA is now in progress; it is not yet marked passed. Physical-device checklist remains pending.

## EPUB and cancellation QA — 2026-09-25
- EPUB Files import passed with noncontiguous One/Three chapter selection, Contents navigation and actual text export. Saved text contains FIRST/THIRD and excludes SECOND. No-TOC EPUB UI passed with its explicit reading-order warning and Section 2 navigation. Corrupt PDF displays its error and does not add a Library document.
- Found and fixed a cancellation classification bug: cancelling during RecognizeDocumentsRequest produced Vision's own cancellation error, which was incorrectly displayed as an import failure. ImportCoordinator now recognizes the user's cancellation/task state as well as Swift CancellationError. Repeated XCTest returned to Library and passed relaunch/no-document assertions; library metadata and managed folders were unchanged.
- The initial cleanup assertion expected no temporary imports at all, but found only an embedded.pdf staging directory dated 2026-09-24 18:43 from yesterday's interrupted test. The current cancellation left no new temporary folder. The harness now compares before/after staging state without deleting older recovery data; this revised assertion will be rerun.
- Remote backup 9abfe98e3fef21763b221dc6e09126369d14ea60 matches the complete tree of local 2616382 exactly. GitHub main remains 5a49a69. Current further QA: multiple distinct photo import, mixed PDF All pages, cancellation cleanup regression, small-screen/iPad/Dynamic Type review.

## Recovery checkpoint — 2026-09-25
- Worktree: `/Users/karloss/Documents/Codex/kokoro-ios-reader-implementation`, branch `codex/native-reader`. This status records the state left after the 2026-09-25 QA run. No new tests were started during recovery.
- Since local checkpoint `2616382`, commit `328c421` preserves the Vision cancellation fix, EPUB and corrupt-PDF UI QA, cancellation regression harness, and photo fixtures. Production change: `ImportCoordinator.finishError` treats a Vision error as cancellation when the user/task requested cancellation. Simulator UI test passed at largest Dynamic Type; Library documents survived relaunch and no new staged import directory or library entry remained. An older staged `embedded.pdf` dated 2026-09-24 was present before the run and was left untouched.
- iPhone 17e Simulator (390 pt, iOS 27): two distinct selected photos passed PhotosPicker → ordered OCR → Reader → Export; exported `[Page 1] FIRST PHOTO` precedes `[Page 2] SECOND PHOTO`. Reader, Library and Settings at `AccessibilityXXXL` were exercised by `testLargeTextLayout`; it passed, and screenshots were captured. In Reader the play and next-sentence controls were hittable. This exceeds the earlier note that maximum-size Reader inspection was still pending.
- Mixed PDF → All pages UI flow also passed on that simulator through Reader page navigation and Export Text. This is Simulator-only; the export’s all-page OCR text was not separately asserted by the harness.
- Current UI batch additionally ran `testEPUBSelectAll`, which failed at `XCTAssertFalse(Import.isEnabled)` after XCTest tapped “All chapters” once. The same batch’s largest Dynamic Type Reader and mixed PDF All pages tests passed. No diagnosis or application change was made for this failure. This is the precise next QA point: inspect whether the All chapters control actually toggled selection in the UI, then complete its on/off behavior before proceeding.
- The two surviving uncommitted files are legitimate UI harness additions: `Tests/ReaderUITests.swift` adds All chapters, max Dynamic Type, PhotosPicker and related flows; `Tests/run-ui.py` supports selecting multiple named UI tests and checks ordered photo export/cancellation cleanup. Preserve these files and checkpoint them with this status. No uncommitted production source changes remain.
- Most recent local source build occurred through the XCTest temporary project on Xcode 27 / iOS 27 Simulator after the cancellation source change; the app target and UI test target built. The latest XCTest batch was not wholly green because of the All chapters assertion above.
- Physical iPhone/iPad QA remains outstanding: actual Apple voices/TTS quality, camera scanner, screen lock/background audio, Control Center/remote commands, interruptions, device memory and large real documents. No simulator result establishes those behaviors.
- Remote feature snapshot is `9abfe98e3fef21763b221dc6e09126369d14ea60`; it exactly matched tree `2616382` when published. It does not include local commit `328c421` or the two uncommitted UI harness files. No new remote backup was made during recovery. GitHub `main` and local `main` / `origin/main` remain `5a49a6959418e2a49ecdb3e7f2c3e55593f13d02`.

## All chapters resolved — 2026-09-25
- Compared center and text-label taps without changing production code: the center left Import enabled; tapping the label deselected every chapter and disabled Import. A second center tap did not select them again. This isolates the transparent spacer hit area in the plain button, rather than selection/Import logic.
- Added a rectangular full-row hit area, minimum 44 pt height and selected accessibility trait. Regression on iPhone 17e Simulator passed: initial selection of all three chapters, center tap to none/disabled Import, individual selection, all again, individual deselection, and only Two imported. The actual exported file contains SECOND and excludes FIRST/THIRD. Source/test build passed.
- Visual review of the older AccessibilityXXXL captures revealed truncated navigation labels and an overly narrow title column in Library despite passing hittability assertions. Next: adapt those layouts, then review screenshots and related navigation, followed by iPad review and final checkpoint/backup. The runtime emits an AVAudioSession synchronous activation/deactivation warning on the main thread; no audio-session behavior change has been made.

## Extreme Dynamic Type layout — 2026-09-25
- Library now stacks thumbnail/title/actions in accessibility sizes so the title is not squeezed into a narrow horizontal column. Reader uses full-size progress text on separate lines and icon navigation with full accessibility labels/Apple Large Content Viewer at accessibility sizes. Normal text-size navigation retains visible captions.
- AccessibilityXXXL XCTest passed on 390 pt iPhone 17e: Library → Reader, next/previous sentence, back to Library and Settings. Screenshots reviewed: navigation labels no longer truncate and content retains its requested large font and scrollable region. No font-size cap was imposed on reading text. Next: iPad geometry/regression, final versioned checkpoint and remote snapshot; physical-device checks remain as listed above.

## Recovery checkpoint after iPad review — 2026-09-25
- Current local HEAD at recovery: `595d6d693d7638ef1448dcc2d519593c43676fa5` (`Adapt library and reading controls to accessibility text sizes`). No production source changes exist after this commit.
- The All chapters row hit-area fix and its iPhone 17e regression are complete: all selected, none selected, individual selection, select all again, deselect One/Three, then import/export only Two. The saved export contained SECOND and excluded FIRST/THIRD. This result is from iPhone Simulator.
- Dynamic Type accessibility layout changes compiled and passed UI checks at 390 pt on iPhone 17e, including Reader navigation, return to Library and Settings. Screenshots were visually reviewed; the reading text remains scrollable at the requested size.
- On iPad Simulator, Add Text → Reader, sentence navigation, playback state/relaunch position and Settings/appearance/voice-list navigation passed after XCTest was changed to locate iPad's floating tabs by accessible button name. Reader was visually reviewed. EPUB chapter selection/import/export was separately validated on iPhone.
- The latest interrupted iPad Files/EPUB XCTest did not finish. Files exposes “On My iPad” in a different and delayed hierarchy from the iPhone “Browse” flow. The run later lost the “All chapters” accessibility query after selecting individual chapters; import and export were not completed in that iPad run. Treat this as an XCTest/system picker automation limitation with no demonstrated production defect. Do not continue this automation in this phase.
- README and the two UI harness files had legitimate local edits after `595d6d6`: focused test-run instructions and accessibility behavior; iPad Files sidebar handling; a separate `selection.epub` fixture; iPad floating-tab lookup; default text/Reader/Settings smoke flow. These were preserved in the subsequent local checkpoint. The last attempted iPad test was not wholly green and no new tests were started during recovery.
- Physical iPhone/iPad validation remains pending: actual voice quality/availability, camera, screen lock/background audio, interruptions and remote controls, real-document performance/memory, and offline behavior with system voice resources. Only iOS 27 Simulator was available; no device or iOS 26 runtime result is claimed.
- Last known remote feature snapshot remains `9abfe98e3fef21763b221dc6e09126369d14ea60`, matching local tree `2616382`; it predates commits `328c421`, `1d15bc3`, `0eff41b`, `595d6d6` and the local README/test harness edits. No remote update was made for this recovery. Local `main` and `origin/main` remain `5a49a6959418e2a49ecdb3e7f2c3e55593f13d02`.
- Handoff point: implementation and feasible iPhone/iPad Simulator checks above are checkpointed. Remaining work for Astra is final status/smoke review and preparation of the physical-device checklist; Files on iPad remains an explicitly deferred automation limitation.
